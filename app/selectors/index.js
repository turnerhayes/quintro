import * as uiSelectors       from "./ui"; 
import * as userSelectors     from "./users"; 
import * as gameSelectors     from "./games"; 
import * as settingsSelectors from "./settings"; 

export const ui = uiSelectors;
export const users = userSelectors;
export const settings = settingsSelectors;
export const games = gameSelectors;